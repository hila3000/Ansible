input {
  beats {
    port => 5044
    host => "0.0.0.0"
  }
}

output {
  elasticsearch {
    hosts => "http://{{ elasticsearch_container_name }}:9200"
    index => "logstash-%{+YYYY.MM.dd}"
  }
}
